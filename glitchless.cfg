exec universal

// Settings
speedrun_save vault

// Aliases

// Binds
bind 4 "sv_player_funnel_into_portals 1"
bind 5 "sv_player_funnel_into_portals 0"
bind 6 "bind mwheelup +jump"
bind 7 "bind mwheelup +use"
bind 3 -duck